const cubicArray= (data )=> {
    const newArr=[];
    data.forEach((item) =>{
        const cube= item**3;
        newArr.push(code);
    };
    return newArr;
};
console.log(cubicArray([2, 4, 5, 7]));